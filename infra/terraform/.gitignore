# Terraform — never commit local state, provider binaries, or crash logs.
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Plan output files — binary, environment-specific, not safe to commit.
tfplan
*.tfplan

# Keep .terraform.lock.hcl committed (it pins provider versions).
