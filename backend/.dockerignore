# Python caches
__pycache__/
*.py[cod]
.pytest_cache/

# Local virtualenvs
.venv/
venv/

# Secrets and local data — must never go into the image
.env
.env.*
*.db
*.sqlite3
media/

# Tests and dev-only files — not needed in the production image
tests/
requirements-dev.txt
pytest.ini

# Dev / VCS noise
.git/
.gitignore
.dockerignore
Dockerfile
README.md
