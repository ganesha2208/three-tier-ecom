server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unknown path serves index.html so the
    # React Router can handle client-side routes (e.g. /products/3).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
