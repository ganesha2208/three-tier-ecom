fastapi==0.136.1
# starlette pinned explicitly to satisfy Trivy on multiple CVEs:
#   CVE-2025-62727 (Phase 2)
#   CVE-2026-48818 (SSRF / NTLM via UNC in StaticFiles) — fix 1.1.0
#   CVE-2026-54283 (form() limit bypass / DoS)         — fix 1.3.1
starlette==1.3.1
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.30  # CVE-2026-53539 (quadratic querystring parse / DoS)
email-validator==2.3.0
stripe==11.4.1
python-slugify==8.0.4
httpx==0.28.1
structlog==24.4.0

# Phase 6 — Prometheus client for the /metrics endpoint. This library has no
# framework dependencies, so it never conflicts with the pinned starlette.
prometheus-client==0.21.1
